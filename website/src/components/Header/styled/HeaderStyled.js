import styled from "styled-components"
import theme from "../../../theme"

const HeaderStyled = styled.header`
  padding: 1rem 0;
  background: ${theme.bgHeader || "#7FFF00"};
`

export default HeaderStyled
